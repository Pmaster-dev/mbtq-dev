{
  "name": "mbtq-workers",
  "version": "1.0.0",
  "private": true,
  "description": "MBTQ.dev Cloudflare Workers — edge and quick functions via Hono",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.7.11"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250801.0",
    "typescript": "^5.3.3",
    "wrangler": "^4.0.0"
  }
}
