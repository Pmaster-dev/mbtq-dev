# ── Core ──────────────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-multipart==0.0.30
httpx==0.27.2

# ── Auth ──────────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.4.0

# ── Computer Vision (local / shared) ──────────────────────────────────────────
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==12.3.0
mediapipe==0.10.14

# ── ML (local transformer model) ──────────────────────────────────────────────
torch==2.6.0
torchvision==0.21.0

# ── Cloud providers (all optional — install only what you need) ────────────────

# AWS
boto3==1.35.0

# Azure
azure-ai-vision-imageanalysis==1.0.0
azure-core==1.38.0

# Google Cloud
google-cloud-videointelligence==2.13.5
google-cloud-vision==3.7.4
google-cloud-aiplatform==1.133.0

# OpenAI
openai==1.51.0
